export default class Shape {
  /**
   * Checks if a point is inside the shape.
   *
   * @param {number} x - X-coordinate of the point.
   * @param {number} y - Y-coordinate of the point.
   * @returns {boolean}
   */
  containsPoint(x, y) {
    throw new Error("containsPoint() has not been implemented.");
  }
}
